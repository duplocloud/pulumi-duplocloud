# WARNING: This file is autogenerated - changes will be overwritten when regenerated by https://github.com/pulumi/ci-mgmt

name: Upgrade provider
on:
  workflow_dispatch:
    inputs:
      version:
        description: |
          The version of the upstream provider to upgrade to, without the 'v' prefix

          If no version is specified, it will be inferred from the upstream provider's release tags.
        required: false
        type: string
      upgradeProviderVersion:
        description: |
          Version of upgrade-provider to use. This must be a valid git reference in the pulumi/upgrade-provider repo. Defaults to "main"

          See https://go.dev/ref/mod#versions for valid versions. E.g. "v0.1.0", "main", "da25dec".
        default: main
        type: string
  schedule:
    # 3 AM UTC ~ 8 PM PDT / 7 PM PST daily. Time chosen to run during off hours.
    - cron: 0 3 * * *

env:
  GH_TOKEN: ${{ secrets.PULUMI_PROVIDER_AUTOMATION_TOKEN || secrets.PULUMI_BOT_TOKEN || secrets.GITHUB_TOKEN }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  PULUMI_GO_DEP_ROOT: ${{ github.workspace }}/..
  PULUMI_LOCAL_NUGET: ${{ github.workspace }}/nuget
  TF_APPEND_USER_AGENT: pulumi

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  upgrade_provider:
    name: upgrade-provider
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          # Persist credentials so upgrade-provider can push a new branch.
          persist-credentials: true
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0'
      - name: Install mise
        uses: jdx/mise-action@v2
        with:
          install: false
      - name: Install upgrade-provider
        run: go install github.com/pulumi/upgrade-provider@${{ inputs.upgradeProviderVersion || 'main' }}
        shell: bash
      - name: "Set up git identity"
        run: |
          git config --global user.name 'bot@pulumi.com'
          git config --global user.email 'bot@pulumi.com'
        shell: bash
      - name: Create issues for new upstream version
        if: inputs.version == ''
        id: upstream_version
        # This step outputs `latest_version` if there is a pending upgrade
        run: upgrade-provider "$REPO" --kind=check-upstream-version
        env:
          REPO: ${{ github.repository }}
        shell: bash
      - name: Calculate target version
        id: target_version
        # Prefer the manually specified version if it exists
        # upstream_version will be empty if the provider is up-to-date
        run: echo "version=${{ github.event.inputs.version || steps.upstream_version.outputs.latest_version }}" >> "$GITHUB_OUTPUT"
        shell: bash
      - name: Call upgrade provider action
        id: upgrade_provider
        if: steps.target_version.outputs.version != ''
        uses: pulumi/pulumi-upgrade-provider-action@12996503de2aaddd72c7426efc43331a8970894f # v0.0.19
        with:
          kind: provider
          email: bot@pulumi.com
          username: pulumi-bot
          automerge: true
          target-version: ${{ steps.target_version.outputs.version }}
          allow-missing-docs: true
          pr-title: "Sync Pulumi provider with terraform-provider-duplocloud v${{ steps.target_version.outputs.version }}"
          pr-body: |
            This PR synchronizes the Pulumi DuploCloud provider with upstream terraform-provider-duplocloud v${{ steps.target_version.outputs.version }}.
            
            ## Changes
            - Updated provider/go.mod to reference terraform-provider-duplocloud v${{ steps.target_version.outputs.version }}
            - Regenerated Pulumi provider schema from Terraform provider
            - Regenerated Pulumi SDKs for all languages:
              - Go
              - Python (PyPI)
              - Node.js (npm)
              - .NET (NuGet)
            
            ## Upstream Release
            See [terraform-provider-duplocloud v${{ steps.target_version.outputs.version }}](https://github.com/duplocloud/terraform-provider-duplocloud/releases/tag/v${{ steps.target_version.outputs.version }}) for details about the changes in the underlying Terraform provider.
            
            ## Note
            This PR will automatically trigger a release of the Pulumi provider when merged, creating:
            - GitHub Release v${{ steps.target_version.outputs.version }}
            - Updated packages on PyPI, npm, and NuGet