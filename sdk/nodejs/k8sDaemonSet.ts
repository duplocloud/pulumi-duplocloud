// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as duplocloud from "@duplocloud/pulumi";
 *
 * const myapp = new duplocloud.Tenant("myapp", {
 *     accountName: "myapp",
 *     planId: "default",
 * });
 * const myappK8sDaemonSet = new duplocloud.K8sDaemonSet("myapp", {
 *     tenantId: myapp.tenantId,
 *     metadata: {
 *         name: "daemonsetname",
 *     },
 *     spec: {
 *         selector: {
 *             matchLabels: {
 *                 app: "myapp",
 *             },
 *         },
 *         template: {
 *             metadata: {
 *                 labels: {
 *                     app: "myapp",
 *                 },
 *             },
 *             spec: {
 *                 containers: [{
 *                     name: "containername",
 *                     image: "nginx:latest",
 *                 }],
 *             },
 *         },
 *         updateStrategy: {
 *             type: "RollingUpdate",
 *             rollingUpdate: {
 *                 maxUnavailable: "1",
 *                 maxSurge: "0",
 *             },
 *         },
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * Example: Importing an existing kubernetes daemonset
 *
 *  - *TENANT_ID* is the tenant GUID
 *
 *  - *NAME* is the k8s daemonset name
 *
 * # 
 *
 * ```sh
 * $ pulumi import duplocloud:index/k8sDaemonSet:K8sDaemonSet myapp v3/subscriptions/*TENANT_ID*&#47;k8s/daemonSet/*NAME*
 * ```
 */
export class K8sDaemonSet extends pulumi.CustomResource {
    /**
     * Get an existing K8sDaemonSet resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: K8sDaemonSetState, opts?: pulumi.CustomResourceOptions): K8sDaemonSet {
        return new K8sDaemonSet(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'duplocloud:index/k8sDaemonSet:K8sDaemonSet';

    /**
     * Returns true if the given object is an instance of K8sDaemonSet.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is K8sDaemonSet {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === K8sDaemonSet.__pulumiType;
    }

    /**
     * When true, the DaemonSet will be deployed only to the tenant's nodes. When false, it is deployed cluster-wide (requires the tenant to have the CAN*DEPLOY*CLUSTER*WIDE*DAEMONSET metadata enabled). Defaults to `true`.
     */
    public readonly isTenantLocal!: pulumi.Output<boolean | undefined>;
    /**
     * Standard daemonset's metadata. More info: https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#metadata
     */
    public readonly metadata!: pulumi.Output<outputs.K8sDaemonSetMetadata>;
    /**
     * Spec of the DaemonSet.
     */
    public readonly spec!: pulumi.Output<outputs.K8sDaemonSetSpec | undefined>;
    /**
     * The GUID of the tenant that the DaemonSet will be created in.
     */
    public readonly tenantId!: pulumi.Output<string>;

    /**
     * Create a K8sDaemonSet resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: K8sDaemonSetArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: K8sDaemonSetArgs | K8sDaemonSetState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as K8sDaemonSetState | undefined;
            resourceInputs["isTenantLocal"] = state ? state.isTenantLocal : undefined;
            resourceInputs["metadata"] = state ? state.metadata : undefined;
            resourceInputs["spec"] = state ? state.spec : undefined;
            resourceInputs["tenantId"] = state ? state.tenantId : undefined;
        } else {
            const args = argsOrState as K8sDaemonSetArgs | undefined;
            if ((!args || args.metadata === undefined) && !opts.urn) {
                throw new Error("Missing required property 'metadata'");
            }
            if ((!args || args.tenantId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'tenantId'");
            }
            resourceInputs["isTenantLocal"] = args ? args.isTenantLocal : undefined;
            resourceInputs["metadata"] = args ? args.metadata : undefined;
            resourceInputs["spec"] = args ? args.spec : undefined;
            resourceInputs["tenantId"] = args ? args.tenantId : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(K8sDaemonSet.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering K8sDaemonSet resources.
 */
export interface K8sDaemonSetState {
    /**
     * When true, the DaemonSet will be deployed only to the tenant's nodes. When false, it is deployed cluster-wide (requires the tenant to have the CAN*DEPLOY*CLUSTER*WIDE*DAEMONSET metadata enabled). Defaults to `true`.
     */
    isTenantLocal?: pulumi.Input<boolean>;
    /**
     * Standard daemonset's metadata. More info: https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#metadata
     */
    metadata?: pulumi.Input<inputs.K8sDaemonSetMetadata>;
    /**
     * Spec of the DaemonSet.
     */
    spec?: pulumi.Input<inputs.K8sDaemonSetSpec>;
    /**
     * The GUID of the tenant that the DaemonSet will be created in.
     */
    tenantId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a K8sDaemonSet resource.
 */
export interface K8sDaemonSetArgs {
    /**
     * When true, the DaemonSet will be deployed only to the tenant's nodes. When false, it is deployed cluster-wide (requires the tenant to have the CAN*DEPLOY*CLUSTER*WIDE*DAEMONSET metadata enabled). Defaults to `true`.
     */
    isTenantLocal?: pulumi.Input<boolean>;
    /**
     * Standard daemonset's metadata. More info: https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#metadata
     */
    metadata: pulumi.Input<inputs.K8sDaemonSetMetadata>;
    /**
     * Spec of the DaemonSet.
     */
    spec?: pulumi.Input<inputs.K8sDaemonSetSpec>;
    /**
     * The GUID of the tenant that the DaemonSet will be created in.
     */
    tenantId: pulumi.Input<string>;
}
