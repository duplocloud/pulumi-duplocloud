// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * `duplocloud.AwsSqsQueue` retrieves an SQS queue in a Duplo tenant.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as duplocloud from "@pulumi/duplocloud";
 *
 * // Example 1 - look up a standard SQS queue by tenant ID and name.
 * const myqueue = duplocloud.getAwsSqsQueue({
 *     tenantId: tenantId,
 *     name: "myqueue",
 * });
 * export const queueArn = myqueue.then(myqueue => myqueue.arn);
 * export const queueUrl = myqueue.then(myqueue => myqueue.url);
 * export const queueFullname = myqueue.then(myqueue => myqueue.fullname);
 * // Example 2 - look up a FIFO SQS queue.
 * const myfifoqueue = duplocloud.getAwsSqsQueue({
 *     tenantId: tenantId,
 *     name: "myfifoqueue.fifo",
 * });
 * export const fifoQueueArn = myfifoqueue.then(myfifoqueue => myfifoqueue.arn);
 * export const isFifoQueue = myfifoqueue.then(myfifoqueue => myfifoqueue.fifoQueue);
 * ```
 */
export function getAwsSqsQueue(args: GetAwsSqsQueueArgs, opts?: pulumi.InvokeOptions): Promise<GetAwsSqsQueueResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("duplocloud:index/getAwsSqsQueue:getAwsSqsQueue", {
        "name": args.name,
        "tenantId": args.tenantId,
    }, opts);
}

/**
 * A collection of arguments for invoking getAwsSqsQueue.
 */
export interface GetAwsSqsQueueArgs {
    /**
     * The short name of the SQS queue. Add `fifo` suffix for FIFO queues.
     */
    name: string;
    /**
     * The GUID of the tenant that the SQS queue resides in.
     */
    tenantId: string;
}

/**
 * A collection of values returned by getAwsSqsQueue.
 */
export interface GetAwsSqsQueueResult {
    /**
     * The ARN of the SQS queue.
     */
    readonly arn: string;
    /**
     * Enables content-based deduplication for FIFO queues.
     */
    readonly contentBasedDeduplication: boolean;
    /**
     * SQS dead letter queue configuration.
     */
    readonly deadLetterQueueConfigurations: outputs.GetAwsSqsQueueDeadLetterQueueConfiguration[];
    /**
     * Specifies whether message deduplication occurs at the message group or queue level.
     */
    readonly deduplicationScope: string;
    /**
     * Postpone the delivery of new messages to consumers for a number of seconds.
     */
    readonly delaySeconds: number;
    /**
     * Boolean designating a FIFO queue.
     */
    readonly fifoQueue: boolean;
    /**
     * Specifies whether the FIFO queue throughput quota applies to the entire queue or per message group.
     */
    readonly fifoThroughputLimit: string;
    /**
     * The full name of the SQS queue.
     */
    readonly fullname: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * The number of seconds Amazon SQS retains a message.
     */
    readonly messageRetentionSeconds: number;
    /**
     * The short name of the SQS queue. Add `fifo` suffix for FIFO queues.
     */
    readonly name: string;
    /**
     * The time for which a ReceiveMessage call will wait for a message to arrive.
     */
    readonly receiveWaitTimeSeconds: number;
    /**
     * The GUID of the tenant that the SQS queue resides in.
     */
    readonly tenantId: string;
    /**
     * The URL for the SQS queue.
     */
    readonly url: string;
    /**
     * The visibility timeout for the queue.
     */
    readonly visibilityTimeoutSeconds: number;
}
/**
 * `duplocloud.AwsSqsQueue` retrieves an SQS queue in a Duplo tenant.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as duplocloud from "@pulumi/duplocloud";
 *
 * // Example 1 - look up a standard SQS queue by tenant ID and name.
 * const myqueue = duplocloud.getAwsSqsQueue({
 *     tenantId: tenantId,
 *     name: "myqueue",
 * });
 * export const queueArn = myqueue.then(myqueue => myqueue.arn);
 * export const queueUrl = myqueue.then(myqueue => myqueue.url);
 * export const queueFullname = myqueue.then(myqueue => myqueue.fullname);
 * // Example 2 - look up a FIFO SQS queue.
 * const myfifoqueue = duplocloud.getAwsSqsQueue({
 *     tenantId: tenantId,
 *     name: "myfifoqueue.fifo",
 * });
 * export const fifoQueueArn = myfifoqueue.then(myfifoqueue => myfifoqueue.arn);
 * export const isFifoQueue = myfifoqueue.then(myfifoqueue => myfifoqueue.fifoQueue);
 * ```
 */
export function getAwsSqsQueueOutput(args: GetAwsSqsQueueOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetAwsSqsQueueResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("duplocloud:index/getAwsSqsQueue:getAwsSqsQueue", {
        "name": args.name,
        "tenantId": args.tenantId,
    }, opts);
}

/**
 * A collection of arguments for invoking getAwsSqsQueue.
 */
export interface GetAwsSqsQueueOutputArgs {
    /**
     * The short name of the SQS queue. Add `fifo` suffix for FIFO queues.
     */
    name: pulumi.Input<string>;
    /**
     * The GUID of the tenant that the SQS queue resides in.
     */
    tenantId: pulumi.Input<string>;
}
